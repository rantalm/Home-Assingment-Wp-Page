export const isMobile = () => window.innerWidth <= 530

/**
 * appData Holds Swiper instances as well (init.js)
 * The transitionTimeOut is used after the play btn paused, and cleard when different slide been clicked.
 */
export const appData = { transitionTimeOut: null }
